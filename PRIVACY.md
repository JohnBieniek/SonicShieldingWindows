# Privacy

Sonic Shielding processes the current Windows output audio in memory on the device. It does not record, retain, transmit, sell, or share audio or profile settings. Settings remain in `%LOCALAPPDATA%\SonicShielding`.

The app makes no network requests during normal operation. Package restoration and release downloads are build/install activities, not runtime telemetry.
